# نتائج ملف الأداء — تحقق محلي

تم تنفيذ مشكلات الأداء الأربع محليًا بالترتيب. لم يُدفع أي تغيير أو يُنشر.

- [x] أضيف `cleanupExpiredMarketSnapshots()` لحذف اللقطات التي انتهت منذ أكثر من 24 ساعة، مع معالج Heartbeat آمن في `/api/scheduled/market-snapshot-cleanup` يسجل عدد الصفوف فقط.
- [x] أضيفت مشاركة Promise الجاري لكل مفتاح كاش لمساري التحليل والشموع، وتُزال المفاتيح بعد النجاح أو الفشل.
- [x] أضيف كاش L1 محلي محدود إلى 500 مفتاح مع انتهاء صلاحية وإخلاء أقدم مفتاح؛ يبقى MySQL مصدر الكاش المشترك بين العمليات.
- [x] أصبحت أدوات المساعد المستقلة في الجولة نفسها تعمل عبر `Promise.all` مع الحفاظ على ترتيب نتائج `tool_call_id` وعزل فشل كل أداة.
- [x] اجتازت الاختبارات الكاملة بعد كل مشكلة؛ آخر تشغيل: 154 اختبارًا ناجحًا و2 متجاوزان، مع نجاح `pnpm check`.

> مهمة Heartbeat الفعلية لا يمكن إنشاؤها قبل نشر معالجها؛ بعد موافقة نشر مستقلة، تُنشأ مهمة مشروعية بالصيغة `0 0 * * * *` لمسار `/api/scheduled/market-snapshot-cleanup` ثم يوثق معرّفها وسجل تنفيذها.
