import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { SUGGESTED_SYMBOLS, SymbolSelect } from "@/components/SymbolSelect";
import { EmptyAction, formatValue, PageHeading, Panel, SignalBadge } from "@/components/market-ui";
import { trpc } from "@/lib/trpc";
import { assessTradeRisk, consumePaperTradeDraft } from "@/lib/paperTradeDraft";
import { AlertTriangle, ArrowDownToLine, ArrowUpFromLine, ChartNoAxesCombined, ShieldAlert, Target, WalletCards } from "lucide-react";
import { FormEvent, useEffect, useMemo, useState } from "react";
import { toast } from "sonner";

type TradeForm = { symbol: string; exchange: string; assetClass: "crypto" | "stock" | "forex" | "futures"; side: "long" | "short"; quantity: string; entryPrice: string; stopLoss: string; takeProfit: string; note: string };
const initialForm: TradeForm = { symbol: "BTCUSDT", exchange: "BINANCE", assetClass: "crypto", side: "long", quantity: "", entryPrice: "", stopLoss: "", takeProfit: "", note: "" };

export default function PaperTrading() {
  const [form, setForm] = useState<TradeForm>(initialForm);
  const [closePrices, setClosePrices] = useState<Record<number, string>>({});
  const utils = trpc.useUtils();
  const trades = trpc.paperTrading.list.useQuery();
  const summary = trpc.paperTrading.summary.useQuery();
  const signalPerformance = trpc.paperTrading.signalPerformance.useQuery();
  const riskAssessment = useMemo(() => assessTradeRisk(form), [form]);
  const refreshTrades = () => { utils.paperTrading.list.invalidate(); utils.paperTrading.summary.invalidate(); };
  const open = trpc.paperTrading.open.useMutation({ onSuccess: () => { toast.success("فُتحت الصفقة الورقية."); setForm(initialForm); refreshTrades(); }, onError: error => toast.error(error.message) });
  const close = trpc.paperTrading.close.useMutation({ onSuccess: result => { toast.success(`أُغلقت الصفقة. الربح/الخسارة المحققة: ${formatValue(result.realizedPnl, 4)}. يمكنك طلب نقد تعليمي اختياري من صفحة نقد الصفقات.`); refreshTrades(); }, onError: error => toast.error(error.message) });
  useEffect(() => { const draft = consumePaperTradeDraft(); if (draft) { setForm(draft); toast.info("حُمّلت مسودة الصفقة من التحليل. راجعها وعدّلها قبل الفتح."); } }, []);
  const submit = (event: FormEvent) => { event.preventDefault(); if (riskAssessment.warnings.length) return toast.error("صحّح تحذيرات المخاطرة والعائد قبل فتح الصفقة."); open.mutate({ ...form, symbol: form.symbol.toUpperCase(), exchange: form.exchange.toUpperCase(), stopLoss: form.stopLoss || undefined, takeProfit: form.takeProfit || undefined, note: form.note || undefined }); };
  const openTrades = trades.data?.filter(trade => trade.status === "open") ?? [];
  const closedTrades = trades.data?.filter(trade => trade.status === "closed") ?? [];
  return <><PageHeading eyebrow="PAPER PORTFOLIO" title="التداول الورقي" description="افتح وأغلق صفقات محاكاة داخل حسابك. تُعزل المراكز والإشارات لكل مستخدم، ولا تُرسل أي أوامر إلى وسيط حقيقي." /><div className="mb-6 grid gap-3 sm:grid-cols-4">{[["إجمالي الصفقات", summary.data?.totalTrades ?? "—"], ["المفتوحة", summary.data?.openTrades ?? "—"], ["نسبة النجاح", summary.data?.winRate === null || summary.data?.winRate === undefined ? "—" : `${summary.data.winRate}%`], ["الربح/الخسارة المحققة", summary.data ? formatValue(summary.data.realizedPnl, 5) : "—"]].map(([label, value]) => <Panel key={String(label)} className="p-4"><div className="flex items-center gap-2 text-xs text-muted-foreground"><ChartNoAxesCombined className="size-3.5 text-primary" />{label}</div><p className="mt-2 font-mono text-lg">{value}</p></Panel>)}</div><Panel className="mb-6 p-4"><div className="flex items-start gap-3"><div className="flex size-9 shrink-0 items-center justify-center rounded-lg bg-sky-400/10 text-sky-300"><Target className="size-4" /></div><div className="min-w-0"><div className="flex flex-wrap items-center justify-between gap-2"><h2 className="font-semibold">متابعة الإشارات المحفوظة</h2><span className="font-mono text-sm text-sky-200">{signalPerformance.data?.winRate == null ? "بانتظار بيانات كافية" : `${signalPerformance.data.winRate}%`}</span></div><p className="mt-1 text-xs leading-5 text-muted-foreground">قُيّمت {signalPerformance.data?.measuredSignals ?? 0} من أصل {signalPerformance.data?.trackedSignals ?? 0} إشارة وفق اتجاه حركة الإغلاق اليومية بعد حفظها. هذا مؤشر متابعة تعليمي مبدئي، وليس توصية أو إثباتًا لفعالية مستقبلية.</p><div className="mt-3 flex flex-wrap gap-2 text-xs"><span className="rounded-md bg-emerald-400/10 px-2 py-1 text-emerald-200">متوافقة: {signalPerformance.data?.successfulSignals ?? 0}</span><span className="rounded-md bg-rose-400/10 px-2 py-1 text-rose-200">غير متوافقة: {signalPerformance.data?.unfavorableSignals ?? 0}</span><span className="rounded-md bg-white/[0.05] px-2 py-1 text-muted-foreground">قيد المتابعة: {signalPerformance.data?.pendingSignals ?? 0}</span></div></div></div></Panel><div className="grid gap-6 xl:grid-cols-[0.8fr_1.2fr]"><Panel><div className="flex items-center gap-3"><div className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-primary/12 text-primary"><WalletCards className="size-5" /></div><div><h2 className="font-semibold">فتح صفقة محاكاة</h2><p className="text-xs text-muted-foreground">راجع كل القيم قبل الفتح؛ هي للتدريب والتوثيق فقط.</p></div></div><form className="mt-6 grid gap-4 sm:grid-cols-2" onSubmit={submit}><SymbolSelect required label="الرمز" value={form.symbol} onChange={symbol => setForm({ ...form, symbol })} onSelect={symbol => { const entry = SUGGESTED_SYMBOLS.find((item: { symbol: string; exchange: string }) => item.symbol === symbol); if (entry) setForm(prev => ({ ...prev, exchange: entry.exchange, assetClass: entry.exchange === "BINANCE" ? "crypto" : entry.exchange === "FX" ? "forex" : "stock" })); }} /><div><Label>البورصة</Label><Input required className="mt-2 bg-white/[0.025] font-mono" value={form.exchange} onChange={event => setForm({ ...form, exchange: event.target.value })} /></div><div><Label>الفئة</Label><Select value={form.assetClass} onValueChange={value => setForm({ ...form, assetClass: value as TradeForm["assetClass"] })}><SelectTrigger className="mt-2 bg-white/[0.025]"><SelectValue /></SelectTrigger><SelectContent>{["crypto", "stock", "forex", "futures"].map(item => <SelectItem key={item} value={item}>{item}</SelectItem>)}</SelectContent></Select></div><div><Label>الاتجاه</Label><Select value={form.side} onValueChange={value => setForm({ ...form, side: value as TradeForm["side"] })}><SelectTrigger className="mt-2 bg-white/[0.025]"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="long">شراء / Long</SelectItem><SelectItem value="short">بيع / Short</SelectItem></SelectContent></Select></div><div><Label>الكمية</Label><Input required inputMode="decimal" className="mt-2 bg-white/[0.025] font-mono" value={form.quantity} onChange={event => setForm({ ...form, quantity: event.target.value })} /></div><div><Label>سعر الدخول</Label><Input required inputMode="decimal" className="mt-2 bg-white/[0.025] font-mono" value={form.entryPrice} onChange={event => setForm({ ...form, entryPrice: event.target.value })} /></div><div><Label>وقف الخسارة (اختياري)</Label><Input inputMode="decimal" className="mt-2 bg-white/[0.025] font-mono" value={form.stopLoss} onChange={event => setForm({ ...form, stopLoss: event.target.value })} /></div><div><Label>جني الربح (اختياري)</Label><Input inputMode="decimal" className="mt-2 bg-white/[0.025] font-mono" value={form.takeProfit} onChange={event => setForm({ ...form, takeProfit: event.target.value })} /></div><div className="sm:col-span-2 rounded-xl border border-sky-400/20 bg-sky-400/[0.06] p-3" aria-live="polite"><div className="flex items-start gap-2"><Target className="mt-0.5 size-4 shrink-0 text-sky-300" /><div><p className="text-sm font-semibold text-sky-100">نسبة العائد إلى المخاطرة</p><p className="mt-1 font-mono text-lg text-sky-200">{riskAssessment.riskRewardRatio ? `${riskAssessment.riskRewardRatio.toFixed(2)} : 1` : "—"}</p><p className="mt-1 text-xs leading-5 text-muted-foreground">{riskAssessment.hasCompletePlan ? "العائد المحتمل لكل وحدة مخاطرة، بناءً على القيم التي أدخلتها." : "أدخل سعر الدخول ووقف الخسارة وجني الربح بقيم منطقية لعرض النسبة."}</p></div></div></div>{riskAssessment.warnings.length ? <div className="sm:col-span-2 rounded-xl border border-amber-400/35 bg-amber-400/[0.10] p-3 text-sm text-amber-100" role="alert"><div className="flex gap-2"><ShieldAlert className="mt-0.5 size-4 shrink-0" /><div><p className="font-semibold">تحذير قبل فتح الصفقة</p><ul className="mt-1 list-disc space-y-1 pr-4 text-xs leading-5 text-amber-100/90">{riskAssessment.warnings.map(warning => <li key={warning}>{warning}</li>)}</ul></div></div></div> : null}<div className="sm:col-span-2"><Label>ملاحظة</Label><Input className="mt-2 bg-white/[0.025]" value={form.note} onChange={event => setForm({ ...form, note: event.target.value })} /></div><Button disabled={open.isPending || riskAssessment.warnings.length > 0} className="min-h-11 sm:col-span-2" type="submit"><ArrowUpFromLine className="ml-2 size-4" />فتح صفقة ورقية</Button>{riskAssessment.warnings.length ? <p className="sm:col-span-2 flex items-center gap-1.5 text-xs text-amber-200"><AlertTriangle className="size-3.5" />يُعاد تفعيل الحفظ بعد تصحيح القيم التحذيرية.</p> : null}</form></Panel><div className="space-y-4"><Panel><div className="mb-5 flex items-center justify-between gap-3"><div><h2 className="font-semibold">المراكز المفتوحة</h2><p className="mt-1 text-xs text-muted-foreground">{trades.isLoading ? "—" : `${openTrades.length} مركز ضمن حسابك`}</p></div><SignalBadge value="neutral" /></div>{trades.isLoading ? <p className="text-sm text-muted-foreground">جارٍ تحميل المراكز…</p> : openTrades.length ? <div className="space-y-3">{openTrades.map(trade => <div key={trade.id} className="rounded-xl border border-white/[0.07] bg-white/[0.02] p-4"><div className="flex flex-col gap-3 min-[420px]:flex-row min-[420px]:items-center min-[420px]:justify-between"><div><p className="font-mono text-sm font-medium">{trade.symbol} <span className="text-muted-foreground">· {trade.exchange}</span></p><p className="mt-1 text-xs leading-5 text-muted-foreground">{trade.side === "long" ? "Long" : "Short"} · كمية {formatValue(trade.quantity, 6)} · دخول {formatValue(trade.entryPrice, 6)}</p></div><div className="flex flex-col gap-2 min-[420px]:flex-row"><Input aria-label="سعر الإغلاق" inputMode="decimal" placeholder="سعر الإغلاق" className="h-11 w-full bg-black/15 font-mono text-sm min-[420px]:h-9 min-[420px]:w-28 min-[420px]:text-xs" value={closePrices[trade.id] ?? ""} onChange={event => setClosePrices({ ...closePrices, [trade.id]: event.target.value })} /><Button size="sm" variant="outline" className="min-h-11 w-full bg-white/[0.03] min-[420px]:min-h-0 min-[420px]:w-auto" disabled={!closePrices[trade.id] || close.isPending} onClick={() => close.mutate({ id: trade.id, closePrice: closePrices[trade.id] })}><ArrowDownToLine className="ml-1 size-3.5" />إغلاق</Button></div></div></div>)}</div> : <EmptyAction title="لا توجد مراكز مفتوحة" description="عند فتح صفقة محاكاة ستظهر هنا مع أداة لإغلاقها وحساب الربح أو الخسارة." href="/analysis" action="راجع التحليل أولًا" />}</Panel><Panel><h2 className="font-semibold">سجل الصفقات المغلقة</h2><div className="mt-4 space-y-2">{closedTrades.length ? closedTrades.slice(0, 8).map(trade => <div key={trade.id} className="flex items-center justify-between gap-3 rounded-lg bg-white/[0.025] px-3 py-2 text-sm"><span className="truncate font-mono">{trade.symbol}</span><span className={Number(trade.realizedPnl) >= 0 ? "shrink-0 font-mono text-emerald-300" : "shrink-0 font-mono text-rose-300"}>{formatValue(trade.realizedPnl, 5)}</span></div>) : <p className="text-sm text-muted-foreground">لا توجد صفقات مغلقة بعد.</p>}</div></Panel></div></div></>;
}
